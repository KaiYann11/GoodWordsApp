# 다음에 할 것

`CLAUDE.md`는 **지켜야 할 규칙**을, 이 파일은 **어디까지 왔고 무엇이 남았는지**를 담습니다.
규칙은 어겼을 때 조용히 망가지는 것이고, 여기 있는 것은 아직 안 한 일입니다.

## 이 앱이 무엇이 되려 하는가

좋은 글귀를 모아 두는 앱으로 시작했지만, 지금 하려는 것은 **일상을 관리하고 돌아보며
삶을 고쳐 나가는 것**입니다. 그 흐름은 이렇게 봅니다.

```
방향  →  계획  →  실행  →  기록  →  돌아보기  →  조정
 ✗       △       ○       ○         ○           △
```

실행·기록·돌아보기는 촘촘합니다. **비어 있는 것은 맨 앞(방향)과 맨 뒤(조정)입니다.**

## 다음 후보 — 값싼 것부터

### 1. 추천 글귀 대조 거르개 · 작음

AI가 준 `suggestedQuote`를 저장 직전에 보관함과 대조해 이미 있는 것이면 걸러 냅니다.

지금은 물음에 "이미 담아 둔 것은 권하지 말라"고 **부탁만 하고 확인은 안 합니다**
(`GrowthReportParser`가 받은 값을 그대로 저장). 모델이 규칙을 안 지키면 그대로 통과합니다.

- 비교는 `SyncDeduplicator.normalize`(띄어쓰기·대소문자 무시)를 그대로 쓰면 됩니다
- 걸렀을 때 그 자리를 **비울지**, **한 번 더 물을지**가 선택지입니다.
  비우면 조용하고 값이 안 듭니다. 다시 물으면 확실하지만 요청이 두 번 나갑니다
- 사용자가 실제로 겪은 문제입니다("추천 글귀가 내가 쓴 것 같다")

### 2. 아이디어 되새김 · 중간

번뜩인 것을 알림·위젯에서 뺐습니다(`type <> :excluded`). 옳은 결정이었지만, 그래서 지금은
**보관함 아이디어 탭에 직접 들어가야만** 다시 만납니다. 묵혀 둔 것이 그대로 묻힙니다.

글귀처럼 음미하는 것이 아니라 *"이거 아직 살아 있나"* 하고 되묻는 것이라 방식이 달라야
합니다. 예: 주 1회 "묵혀 둔 아이디어 3개" — 각각 **살리기 / 루틴으로 / 지우기**.

### 3. 방향·영역 · 큼 (정체성에 가장 맞음)

루틴이 여럿 있는데 **무엇을 위한 것인지**가 앱 어디에도 없습니다. `category`가 문자열로
있을 뿐입니다. 그래서 AI 돌아보기도 "어디로 가고 싶은지"를 모른 채 남은 기록만 보고 씁니다.

삶의 영역(건강·관계·일·배움)이나 올해의 뜻을 적어 두고 루틴·할 일·책이 그 아래 매달리게
하면, 통계가 *"몇 번 했나"*에서 **"어디로 가고 있나"**로 바뀝니다.

새 레코드 종류라 `CLAUDE.md`의 "여섯 군데" 규칙이 전부 걸리고 마이그레이션이 붙습니다.

### 4. 안 하기로 한 것 · 생각보다 큼

루틴은 늘 "할 것"입니다. 삶 개선은 더하기만큼 빼기가 큰데(야식·자기 전 폰) 표현할 방법이
없습니다.

**처음 봤을 때보다 비쌉니다.** `routineChecks`를 "실천했다"로 세는 곳이 다섯입니다 —
`DailyLoop` · `StatsSummary` · `FeedbackWriter` · `MoodPractice` · `GrowthPrompt`.
안 할 것은 체크의 뜻이 뒤집히므로(`오늘은 안 했다` 또는 `오늘 어겼다`) 다섯 곳을 다
손봐야 합니다. 하나만 빠뜨리면 야식 먹은 날이 실천 1회로 잡힙니다.

### 5. 주간 리듬 · 중간

하루(`DailyLoop`)와 장기(통계·AI)는 있는데 그 사이가 비어 있습니다. 사람이 삶을 **조정하는
단위는 주**입니다. 할 일에 `THIS_WEEK` 칸은 있지만 주간 회고가 없습니다.

AI 돌아보기가 이미 주간 주기를 지원하니, **사람이 직접 5분 쓰는 주간 회고**를 붙이면 짝이
맞습니다. 일요일 저녁 알림 → 세 물음(뭐가 잘 됐나 / 뭐가 막았나 / 다음 주 한 가지).

### 6. 위젯에서 기분 찍기 · 작음

위젯에 "적기"는 붙였지만 기분은 아직 앱을 열어야 합니다. 3초짜리인데 앱을 여는 것이
그보다 오래 걸립니다.

## 미룬 것과 그 이유

다시 꺼낼 때 같은 자리에서 헤매지 않도록 남깁니다.

- **AI 웹 검색 추천** — Anthropic에는 서버 측 `web_search`가 있어 새 글귀·영상을 실제로
  찾아올 수 있습니다. OpenAI를 부르는 방식(chat completions)에는 없어 **공급자마다 결과가
  달라집니다.** 영상을 담으려면 `GrowthReportEntity`에 자리가 없어 마이그레이션도 붙습니다.
  사용자가 "검색 안 해도 되니 알고 있는 것으로"라고 정해서 물음만 고쳤습니다
- **돌아보기 가이드를 다음 물음에 싣기** — 지금은 `improvements`와 `suggestedRoutines`만
  싣습니다. `guide`는 자유롭게 쓴 글이라 그때 읽은 일기가 묻어날 수 있고, 사용자가 그 뒤로
  일기 본문 보내기를 껐다면 껐다는 뜻이 지난 글을 통해 뒤집힙니다. 제대로 막으려면
  리포트마다 "그때 일기 본문이 실렸는지"를 남겨야 하고, 그것이 마이그레이션입니다
- **웹에서 기분·돌아보기 고치기** — 둘 다 읽기만 합니다. 기분은 그날 그때 찍는 것이라
  나중에 웹에서 고치면 그때가 아니라 지금 기억하는 것이 남습니다
- **하루의 걸음 기본값 바꾸기** — 글귀가 여전히 첫 걸음입니다. 기본을 바꾸면 쓰던 사람의
  연속 날수가 업데이트만으로 달라져서, 고르는 길만 열어 두고 기본은 그대로 뒀습니다

## 알아 두면 좋은 것

- **연속 날수는 저장하지 않습니다.** 매번 기록에서 다시 셉니다. 그래서 걸음을 바꾸면 지난
  날수도 곧바로 새 기준이 됩니다
- **`schemaVersion`과 Room 버전은 함께 움직입니다.** 지금 둘 다 16입니다
- 폰은 SM-S921N(`R3CX70R5G3M`), 에뮬레이터는 `AppGoodWords_API_35`입니다.
  에뮬레이터가 자주 죽습니다 — 계측은 종류별로 나눠 돌리세요
